# Roadmap

## Wave 1 (this release)
Plugin surface that passes `muse plugins validate`, role skills, orchestration commands, skill-gate + Ralph stop-chain, `.omm/`.

## Wave 2
Companion CLI beyond stubs: `omm team`, `ask`, `hud`, `wait`. Honest Muse-native team via `subagent_spawn` + `.muse/worktrees/`, not tmux.

## Wave 3
`wiki` / mission persistence, notifications (if Muse has a real surface), tighter verify evidence, more OMC extras **only** when they map to Muse APIs.

## Will not
- Proxy Meta inference
- Copy OMC/OMX skill text
- Reimplement Muse `/loop` or `/compact`
- Occupy `@muse-code/*`
